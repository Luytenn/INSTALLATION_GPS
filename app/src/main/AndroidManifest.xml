<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="net.larntech">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:label="InstalacionGPS"
        android:name=".common.MyApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.RegistroOperacion"></activity>
        <activity android:name=".ui.RegistroVehiculo"></activity>
        <activity
            android:name=".ui.IngresoIMEI"
            android:parentActivityName=".ui.DetalleTareas">

        </activity>
        <activity android:name=".ui.DetalleTareas" />
        <activity android:name=".ui.ListadoTareas" />
        <activity android:name=".ui.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>